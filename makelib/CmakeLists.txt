project(CMakeLearnLib CXX)

cmake_minimum_required(VERSION 3.10)

set(CMAKE_CXX_STANDARD 14)

file(GLOB SRCPATH ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp)

include_directories(${LIBARY_INCLUDE_PATH})

# set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/build/lib)

# add_executable(CMakeLearn ${SRCPATH})
add_library(CMakeLearnLib STATIC ${SRCPATH})

set_target_properties(CMakeLearnLib PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/build/lib
    ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/build/lib
)